apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  type: NodePort
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 4499        # internal port
      targetPort: 4499  # container port
      nodePort: 30080   # external port for Minikube

